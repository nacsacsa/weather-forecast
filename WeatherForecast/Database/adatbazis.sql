create table weather_forecast(
id int,
username varchar(250) not null,
password varchar (250) not null,
constraint pk_id primary key(id));

CREATE DATABASE my_database;
USE my_database;weather_forecast